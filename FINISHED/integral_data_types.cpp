#include <iostream>
#include <cmath>
#include <climits>
//to check storage of data types

int main()
{
    short a;
    int b;
    long c;
    long long d;
    //short <= int <= long <= long long

    unsigned short aa;
    unsigned int bb;
    unsigned long cc;
    unsigned long long dd;
    //unsigned have double the capacity but only store positive

    std::cout << INT_MAX;
    //for checking bits

}