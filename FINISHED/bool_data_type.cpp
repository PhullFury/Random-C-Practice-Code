#include <iostream>

int main()
{
    bool pizza_is_good = 0;
    //1=true 0=false
    //can be used for if statements and search algorithim

    std::cout << pizza_is_good;

    bool found = false;

    std::cout << std::boolalpha << found;
    //std::boolalpha is used if false or true is to be printed
}